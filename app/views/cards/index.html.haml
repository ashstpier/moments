.card-list
  - @cards.each do |card|
    .card-list-item
      = link_to card_path(card) do
        .card{style: "background-image: url(#{image_url('dummy.jpg')})"}
          .card-wrapper
            .card-content
              .card-label
                .name= card.name
                .date= card.formatted_date

              .card-meta
                .likes
                  %i.fa.fa-heart-o
                  25
                .friends
                  %i.fa.fa-user
                  6

= link_to "new card", new_card_path, class: 'btn'